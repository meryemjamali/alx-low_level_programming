#include "main.h"
#include <unistd.h>
/**
 *main - prints 10 times the alphabet, in lowercase, followed by a new line
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	print_alphabet_x10();
	return (0);
}
